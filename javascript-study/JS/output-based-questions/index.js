
//1.higher order function************
 /*function getName(myName){
    const name="chandra praksh"
    return myName(name);
}

const myName=(name)=>{
   console.log(name);
}
getName(myName);*/

//2. const str1 = "abc"; const str2="123456" or str1="123456"; str2="abc" ,
 //output shoud be same in both cases
// output="a1b2c3456";*****************

/*
let res="";
let shorter=str1.length<=str2.length?str1:str2;
let longer=str2.length>str1.length?str2:str1;
const strLength=Math.max(shorter.length,longer.length);

for(let i=0;i<strLength;i++){
    if(i<shorter.length){
        res+=shorter[i];
    }
    if(i<longer.length){
        res+=longer[i];
    }
}
console.log(res);*/









